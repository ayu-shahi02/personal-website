---
import BaseLayout from '../layouts/BaseLayout.astro';
const base = import.meta.env.BASE_URL;

const slides = [
  { src: "DSC_7569_1.jpg", caption: "A ferry ride from Sultan Batteri in Mangalore, India (shot on 35mm film)" },
  { src: "_1182.jpg", caption: "Hong Kong's skyline from the Victoria Peak Loop trail (shot on 35mm film)" },
  { src: "~1126.jpg", caption: "Random skyscrapers in NYC (shot on 35mm film)" },
  { src: "~1138.jpg", caption: "My favorite street in New Haven (shot on 35mm film)" },
];
---

<BaseLayout
  title="Personal - Ayush Shahi"
  description="Personal interests and hobbies of Ayush Shahi"
  currentPage="personal"
>
  <div class="gallery-container">
    <div class="gallery">
      <button class="gallery-nav prev" onclick="changeSlide(-1)" aria-label="Previous image">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m15 18-6-6 6-6"/></svg>
      </button>
      <div class="gallery-images">
        {slides.map((slide, index) => (
          <img
            src={`${base}images/personal/${slide.src}`}
            alt={slide.caption}
            class={`gallery-slide ${index === 0 ? 'active' : ''}`}
            data-caption={slide.caption}
          />
        ))}
      </div>
      <button class="gallery-nav next" onclick="changeSlide(1)" aria-label="Next image">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
      </button>
    </div>
    <p class="gallery-caption">{slides[0].caption}</p>
  </div>

  <h2 class="section-header">Mentorship</h2>

  <p>
    I believe that supportive mentorship can help solve for some of the information barriers that limit access
    to career and educational opportunities, especially for those from marginalized backgrounds. When I decided
    to study economics further, I benefited greatly from my privileged access to people in the know. I now hope to pay it forward:
    feel free to reach out at <a href="mailto:ayush.shahi@yale.edu">ayush.shahi@yale.edu</a> if you have any queries
    about graduate school applications. I will try to help out in whatever way I can.
  </p>

   <p>
    I am currently volunteering as a mentor with <a href="https://bahujanecon.org" target="_blank" rel="noopener">Bahujan Economists</a>,
    an organization working to increase representation of historically marginalized castes, tribes, and religious communities in Economics research.
  </p>

  <h2 class="section-header">Resources for Economics Graduate School</h2>

  <ul>
    <li>
      <a href="https://hackmd.io/@rdrsas/econgradsch" target="_blank" rel="noopener">A Guide to Econ Grad School Applications (esp. for Indians)</a>
    </li>
    <li>
      <a href="https://magenta-editorial-197.notion.site/How-2-GRE-fe97eacfa516410789977f214de94fdb" target="_blank" rel="noopener">How to prepare for the GRE</a>
    </li>
    <li>
      <a href="https://www.youtube.com/@TheTestedTutor/playlists" target="_blank" rel="noopener">The Tested Tutor's GRE quantitative preparation playlist</a>
    </li>
  </ul>

</BaseLayout>

<style>
  .gallery-container {
    margin-bottom: var(--spacing-lg);
  }

  .gallery {
    position: relative;
    width: 100%;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .gallery-images {
    flex: 1;
    position: relative;
    height: 400px;
    overflow: hidden;
    border-radius: 8px;
    background: var(--color-background-alt);
  }

  .gallery-slide {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .gallery-slide.active {
    opacity: 1;
  }

  .gallery-nav {
    background: var(--color-background-alt);
    border: 1px solid var(--color-border);
    border-radius: 50%;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    color: var(--color-text);
    transition: background-color 0.2s ease, color 0.2s ease;
    flex-shrink: 0;
  }

  .gallery-nav:hover {
    background: var(--color-accent);
    color: white;
    border-color: var(--color-accent);
  }

  .gallery-caption {
    font-size: 0.9rem;
    color: var(--color-text-light);
    font-style: italic;
    margin-top: var(--spacing-xs);
    text-align: center;
  }

  @media (max-width: 600px) {
    .gallery-images {
      height: 280px;
    }

    .gallery-nav {
      width: 32px;
      height: 32px;
    }

    .gallery-nav svg {
      width: 18px;
      height: 18px;
    }
  }
</style>

<script is:inline>
  let currentSlide = 0;
  const slides = document.querySelectorAll('.gallery-slide');
  const caption = document.querySelector('.gallery-caption');

  function changeSlide(direction) {
    slides[currentSlide].classList.remove('active');
    currentSlide = (currentSlide + direction + slides.length) % slides.length;
    slides[currentSlide].classList.add('active');
    caption.textContent = slides[currentSlide].dataset.caption;
  }
</script>
